package com.proxiad.holidaysapp.service;


import org.springframework.boot.test.context.SpringBootTest;

/**
 * todo - что делает интеграционный тест и какие нужны зависимости
 */

//@Testcontainers
@SpringBootTest
class ChatServiceFullIntegrationTest {

//    @JavaDispatcher.Container
//    static MockServerContainer mockOpenAI = new MockServerContainer(
//            DockerImageName.parse("mockserver/mockserver:5.15.0"));
//
//    @DynamicPropertySource
//    static void configureProperties(DynamicPropertyRegistry registry) {
//        registry.add("openai.base-url",
//                () -> "http://" + mockOpenAI.getHost() + ":" + mockOpenAI.getServerPort());
//    }
}
